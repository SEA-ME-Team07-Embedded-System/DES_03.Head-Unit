# Connect to any open network
# network={
#    key_mgmt=NONE
#}

# Connect to a WPA2 protected network
network={
      ssid="TP-Link_77CE"
      proto=WPA2
      key_mgmt=WPA-PSK
      pairwise=CCMP TKIP
      group=CCMP TKIP
      scan_ssid=1
      psk="32976700"
      priority=17
}
